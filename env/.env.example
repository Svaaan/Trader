# Copy to env/.env and fill in.

# Where HelloWorldAi lives. The public deployment by default.
TRADER_HELLOWORLD_URL=https://artificialintelligentduck.duckdns.org

# The account. Anyone holding this can see these jobs and download the models
# they produce, so it is not committed.
#
# To share a workspace with the HelloWorldAi UI, use the `submitterKey` value
# from the key file that page saves, and the runs appear in both. To keep them
# separate, generate one:
#
#   python -c "import secrets;print(secrets.token_urlsafe(48)[:64])"
TRADER_SUBMITTER_KEY=
